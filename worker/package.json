{
  "name": "mykalender-ai-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for the GitHub Models AI feature",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
